---
- hosts: localhost
  become: yes
  vars:
    cwm_aws:
      access_key: 'test'
      secret_key: 'test'
  roles:
    - cloudwatch-mon-py
  pre_tasks:
    - yum: name=epel-release update_cache=yes
    - yum: name=python-pip update_cache=yes